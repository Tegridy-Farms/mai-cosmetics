export const dateRegex = /^\d{4}-\d{2}-\d{2}$/;
/** Lowercase slug segments separated by single `-` or `_` (matches campaign/form placeholders). */
export const slugRegex = /^[a-z0-9]+(?:[-_][a-z0-9]+)*$/;
